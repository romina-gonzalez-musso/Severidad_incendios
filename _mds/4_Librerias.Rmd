---
always_allow_html: true
output:
  github_document
---

## **LIBRERÍAS Y REQUERIMIENTOS**

### **1. Google Earth Engine (GEE)**

```{r, echo=FALSE, out.width="25%", fig.cap=""}
knitr::include_graphics("https://github.com/romina-gonzalez-musso/Severidad_incendios/blob/main/_images/Logo_GEE.jpg")
```

Es necesario contar con una cuenta de usuario de Google Earth Engine con un Cloud Project habilitado.
En este repositorio se demostrará el uso de GEE a través de Python. Sin embargo, el mismo procedimiento puede reproducirse utilizando el Code Editor nativo de GEE, que funciona en lenguaje *JavaScript*.

### **2. Python - Dos opciones:**
```{r, echo=FALSE, out.width="22%", fig.cap=""}
knitr::include_graphics("https://github.com/romina-gonzalez-musso/Severidad_incendios/blob/main/_images/Logo_Python.png")
```


#### **2a. Con Google Colab** *(recomendado para principiantes)*
```{r, echo=FALSE, out.width="19%", fig.cap=""}
knitr::include_graphics("https://github.com/romina-gonzalez-musso/Severidad_incendios/blob/main/_images/Logo_colab.jpg")
```

Google Colab es una plataforma en la nube que permite escribir y ejecutar código en Python, especialmente útil para tareas de ciencia de datos. Solo se necesita una cuenta de Gmail para acceder. Colab ya incluye herramientas como Google Earth Engine (GEE) y la librería geemap preinstaladas, lo que facilita el trabajo sin necesidad de instalar nada en la computadora. Además, permite exportar productos (por ejemplo, imágenes) directamente a Google Drive.


#### **2b. En instalación local** *(recomendado para usuarios avanzados)*
```{r, echo=FALSE, out.width="20%", fig.cap=""}
knitr::include_graphics("https://github.com/romina-gonzalez-musso/Severidad_incendios/blob/main/_images/Logo_jupyter.png")
```

También es posible trabajar con Google Earth Engine de forma local utilizando Python. Para ello, solo es necesario instalar la librería [*geemap*](https://geemap.org/) y descargar los notebooks de Jupyter en el directorio local. Esta opción permite ejecutar los análisis directamente desde la computadora, manteniendo una estructura similar a la de Colab.


### **3. R y librerías**
```{r, echo=FALSE, out.width="12%", fig.cap=""}
knitr::include_graphics("https://github.com/romina-gonzalez-musso/Severidad_incendios/blob/main/_images/Logo_R.png")
```

Se necesitan las siguientes librerías de R:

```{r Test, message=FALSE, warning=FALSE, paged.print=FALSE, eval = FALSE}

# Instalar
install.packages("terra")
install.packages("sf")
install.packages("dplyr")
install.packages("raster")

# Probar que se hayan instalado bien
library("terra")
library("sf")
library("dplyr")
library("raster")

```